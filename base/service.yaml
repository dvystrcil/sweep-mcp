apiVersion: v1
kind: Service
metadata:
  name: sweep-mcp
  namespace: sweep-mcp
  labels:
    app.kubernetes.io/name: sweep-mcp
    app.kubernetes.io/part-of: homelab-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sweep-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
