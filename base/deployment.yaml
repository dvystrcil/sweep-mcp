# sweep-mcp Deployment.
#
# Single-replica Go MCP server. Stateless — the only outbound connection
# is to the in-cluster n8n model-sweep-kickoff webhook. Same probe pattern
# as tor-character-mcp.
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    reloader.stakater.com/auto: "true"
  name: sweep-mcp
  namespace: sweep-mcp
  labels:
    app.kubernetes.io/name: sweep-mcp
    app.kubernetes.io/part-of: homelab-mcp
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: sweep-mcp
  template:
    metadata:
      labels:
        app.kubernetes.io/name: sweep-mcp
        app.kubernetes.io/part-of: homelab-mcp
    spec:
      automountServiceAccountToken: false
      imagePullSecrets:
        - name: sweep-mcp-harbor-pull
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: sweep-mcp
          image: harbor.sirddail.net/ai/sweep-mcp:dev
          imagePullPolicy: Always
          args:
            - --http=:8080
          env:
            # Override default if n8n moves namespaces or you need to
            # point at a staging instance.
            - name: N8N_KICKOFF_URL
              value: "http://n8n.n8n-workflow.svc.cluster.local/webhook/model-sweep-kickoff"
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          # D-012 (VPA epic homelab#455): requests right-sized from the
          # Off-mode VPA's measured target. See notes in the PR body.
          resources:
            requests:
              cpu: 25m
              memory: 250Mi
            limits:
              cpu: 200m
              memory: 320Mi
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            failureThreshold: 30
            periodSeconds: 2
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 30
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
